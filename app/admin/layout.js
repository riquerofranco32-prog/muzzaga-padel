export const metadata = {
  title: "Administración · Muzzaga Pádel",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
